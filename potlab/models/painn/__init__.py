"""PaiNN (Schütt et al., 2021): core (scriptable, graph-agnostic) + model (graph builder).

``core.py`` holds the pure-PyTorch network; ``model.py`` (M2 step 4) adds
the PyG graph builder and registers the model.
"""
